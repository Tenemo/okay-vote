[build]
base = "."
command = "test -n \"$API_BASE_URL\" || (echo \"API_BASE_URL must point at the Railway API origin\" && exit 1); pnpm --filter @okay-vote/web build"
publish = "apps/web/dist"

[build.environment]
NODE_VERSION = "24.14.1"
